import styled from 'styled-components';
import {
  Header as RawHeader
} from '@everlywell/leaves';

export const Header = styled(RawHeader)`
  position: relative;
  height: 58px;

  div:first-child {
    height: 37px;
  }
`